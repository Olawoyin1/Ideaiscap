import { Link } from "react-router-dom";

const Purpose = () => {
  return (
    <div>
      <div>
        <div className="text-center gap-4 flex-col min-h-[100vh] flex items-center justify-center  bg-amber-600">
          <h3 className="text-4xl font-bold">page in progress</h3>
          <Link to="/">Back to home page</Link>
        </div>
      </div>
    </div>
  );
};

export default Purpose;
